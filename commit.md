# Черновик коммита

## Сообщение

test: добавить поведенческие проверки лендинга

## Изменения

- Добавлен `tests/site.test.js` с контрактными и поведенческими тестами `index.html`.
- Встроенная логика лендинга вынесена в тестируемый `site.js` без изменения поведения.
- Покрыты структура страницы, ссылки, контакты, изображения, доступность полей формы и безопасность внешних ссылок.
- Покрыты ветви встроенного JavaScript для навигации и формы консультации.
- `tools/check.ps1` запускает тесты, GitHub Actions использует Node.js 22.

## Проверки

- [ ] `node --experimental-test-coverage --test tests/site.test.js`
- [ ] `pwsh -NoProfile -File tools/check.ps1`
- [ ] `pwsh -NoProfile -File tools/doc_check.ps1`

## Примечания

- Коммит не создан: пользователь не просил выполнять `git commit`.
