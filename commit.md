# Черновик коммита

## Сообщение

docs: добавить инструкции codex для проекта

## Изменения

- Добавлен `AGENTS.md` с правилами работы Codex в проекте.
- Добавлен `log_project.md` как журнал проектных изменений.
- Добавлен `commit.md` как черновик коммита.
- Исправлен `tools/doc_check.ps1`, чтобы расширенная проверка не падала при отсутствии untracked-файлов.
- `AGENTS.md`, `log_project.md` и `commit.md` добавлены в обязательные проверки проекта.

## Проверки

- [x] `pwsh -NoProfile -File tools/check.ps1`
- [x] `pwsh -NoProfile -File tools/doc_check.ps1`

## Примечания

- Коммит не создан: пользователь подтвердил выполнение изменений, но не просил делать `git commit`.
- Предупреждения проверок относятся к существующим файлам `docs-salebot`, крупным бинарным файлам и текущим плейсхолдерам лендинга.
