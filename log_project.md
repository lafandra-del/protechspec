# Журнал проекта

## 2026-07-17

### Раздел с ценами на лендинге

- Добавлен отдельный раздел «Цены» с тремя категориями: лендинги, Telegram-боты, интеграции и связки.
- Для каждой категории указаны уровни сложности, состав работ и ценовые диапазоны.
- В навигацию добавлена ссылка на раздел с ценами.
- Добавлены адаптивные стили карточек и CTA для получения точной оценки.
- Добавлен регрессионный тест на наличие трёх категорий и всех 12 ценовых диапазонов.

### Проверки

- `pwsh -NoProfile -File tools/check.ps1`: выход 0, пройдено 11 из 11 тестов, обязательных ошибок нет.
- `pwsh -NoProfile -File tools/doc_check.ps1`: выход 0, обязательных ошибок нет; предупреждения относятся к существующей импортированной документации и крупным файлам.
- `git diff --check`: ошибок пробельного форматирования нет.
- `index.html` проверен в Chromium при ширине 1440 px и 390 px: карточки, навигация и адаптивная раскладка отображаются корректно.

### Усиление CI и деплоя

- Убрана несовместимая с Node.js 22 опция `--test-isolation=none`.
- GitHub Actions закреплены на полных commit SHA и ограничены разрешением `contents: read`.
- Deploy workflow переведён с root на отдельного пользователя `deploy`, production environment, проверку SSH fingerprint и последовательные деплои.
- Добавлены воспроизводимые конфигурации deploy-команды, sudoers и security-заголовков Nginx.
- Приватный SSH-ключ не хранится в репозитории и должен быть добавлен только в GitHub Environment Secret.

### Проверки усиления

- `pwsh -NoProfile -File tools/check.ps1`: выход 0, пройдено 11 из 11 тестов.
- `pwsh -NoProfile -File tools/doc_check.ps1`: выход 0, обязательных ошибок нет.
- Deploy-ключ проверен попыткой выполнить произвольную команду: SSH принудительно выполнил только `/usr/local/sbin/deploy-landing`.
- `visudo -cf`: правило для пользователя `deploy` корректно.
- `nginx -t`: конфигурация корректна; Nginx успешно перезагружен.
- Живой сайт возвращает все пять настроенных security-заголовков и сохраняет HTTP 200.
- Автоматический deploy будет проверен после добавления `SSH_PRIVATE_KEY` в GitHub Environment `production`.
- Первый запуск Deploy для `a8e430b` выявил несовпадение host fingerprint: `drone-ssh 1.7.3` согласовал ECDSA-ключ VPS, а workflow проверял ED25519-ключ.
- В workflow закреплён проверенный ECDSA SHA256 fingerprint сервера; приватные ключи не изменялись и не выводились.

## 2026-07-16

### Поведенческие тесты лендинга

- Проверено текущее покрытие: GitHub Actions запускал только файловые и Markdown-проверки, поведение `index.html` тестами не покрывалось.
- Добавлены тесты на встроенном `node:test` без npm-зависимостей.
- Встроенный JavaScript без изменения поведения вынесен в `site.js`, чтобы исполняемый код измерялся coverage-инструментом.
- Зафиксированы контракты ключевых разделов, внутренних ссылок, контактов, изображений, подписей формы и безопасности ссылок с `target="_blank"`.
- Покрыты ветви JavaScript: переключение активной навигации, ошибки каждого обязательного поля формы и успешная отправка с нормализацией данных.
- Тесты подключены к `tools/check.ps1`, а CI фиксирует Node.js 22 через `actions/setup-node`.

### Проверки

- Результаты будут заполнены после запуска базовой, расширенной и coverage-проверок.

## 2026-07-05

### Адаптация проекта под Codex

- Проверен `CLAUDE.MD` и сопутствующая документация проекта.
- Выявлено, что для Codex нужен отдельный `AGENTS.md` в корне проекта.
- Добавлен `AGENTS.md` с правилами проекта, стеком, проверками, Git-процессом и ограничениями по контенту.
- Создан `commit.md` для черновика будущего коммита.
- Исправлен `tools/doc_check.ps1`, который падал на пустом списке untracked-файлов.
- `AGENTS.md`, `log_project.md` и `commit.md` добавлены в обязательные файлы базовой и расширенной проверок.
- Служебные файлы Codex исключены из рекомендаций `doc_check` по kebab-case и переносу из корня.

### Проверки

- `tools/check.ps1` до изменений проходил без ошибок.
- `tools/doc_check.ps1` до изменений падал на обращении к `$untracked.Count`.
- `pwsh -NoProfile -File tools/check.ps1` после изменений: обязательные проверки пройдены, остались старые предупреждения по markdown-файлам `docs-salebot`.
- `pwsh -NoProfile -File tools/doc_check.ps1` после изменений: выход 0, обязательные проверки пройдены; остались предупреждения по импортированной документации, крупным бинарным файлам и существующим плейсхолдерам.
