# Журнал проекта

## 2026-07-16

### Поведенческие тесты лендинга

- Проверено текущее покрытие: GitHub Actions запускал только файловые и Markdown-проверки, поведение `index.html` тестами не покрывалось.
- Добавлены тесты на встроенном `node:test` без npm-зависимостей.
- Встроенный JavaScript без изменения поведения вынесен в `site.js`, чтобы исполняемый код измерялся coverage-инструментом.
- Зафиксированы контракты ключевых разделов, внутренних ссылок, контактов, изображений, подписей формы и безопасности ссылок с `target="_blank"`.
- Покрыты ветви JavaScript: переключение активной навигации, ошибки каждого обязательного поля формы и успешная отправка с нормализацией данных.
- Тесты подключены к `tools/check.ps1`, а CI фиксирует Node.js 22 через `actions/setup-node`.

### Проверки

- Результаты будут заполнены после запуска базовой, расширенной и coverage-проверок.

## 2026-07-05

### Адаптация проекта под Codex

- Проверен `CLAUDE.MD` и сопутствующая документация проекта.
- Выявлено, что для Codex нужен отдельный `AGENTS.md` в корне проекта.
- Добавлен `AGENTS.md` с правилами проекта, стеком, проверками, Git-процессом и ограничениями по контенту.
- Создан `commit.md` для черновика будущего коммита.
- Исправлен `tools/doc_check.ps1`, который падал на пустом списке untracked-файлов.
- `AGENTS.md`, `log_project.md` и `commit.md` добавлены в обязательные файлы базовой и расширенной проверок.
- Служебные файлы Codex исключены из рекомендаций `doc_check` по kebab-case и переносу из корня.

### Проверки

- `tools/check.ps1` до изменений проходил без ошибок.
- `tools/doc_check.ps1` до изменений падал на обращении к `$untracked.Count`.
- `pwsh -NoProfile -File tools/check.ps1` после изменений: обязательные проверки пройдены, остались старые предупреждения по markdown-файлам `docs-salebot`.
- `pwsh -NoProfile -File tools/doc_check.ps1` после изменений: выход 0, обязательные проверки пройдены; остались предупреждения по импортированной документации, крупным бинарным файлам и существующим плейсхолдерам.
